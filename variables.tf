# Input variable definitions

variable "aws_region" {
  description = "AWS region for all resources."
  type    = string
}

variable "account_id" {
  description = "AWS region for all resources."
  type    = string
}